import { AddressInfo } from '@app/_/AddressInfo';

const Home = () => (
  <main className="dark mx-auto h-full max-w-xl space-y-5 p-2 sm:p-5 md:p-10">
    <AddressInfo />
  </main>
);

export default Home;
